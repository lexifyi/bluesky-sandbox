services:
  main:
    image: node
    entrypoint: ["/bin/bash", "-c"]
    command: ["cd /app && node src/helloWorldFeed.ts"]
    restart: unless-stopped
    ports:
      - 9396:9396
    volumes:
      - .:/app